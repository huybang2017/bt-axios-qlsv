// Định nghĩa Product constructor

function Product(id, name, price, img, description) {
  // Khai báo các thuộc tính (properties)
  this.id = id;
  this.name = name;
  this.price = price;
  this.img = img;
  this.description = description;
}

// Khai báo các phương thức (method)
// Product.prototype.funcName = function() {}
